[modular_input://dummy]
*Only the scheme name is used by Splunk. It must match the executable name.

username =
*Only the parameter name is used by Splunk. It must match what's returned by the executable through introspection.

password =
*Only the parameter name is used by Splunk. It must match what's returned by the executable through introspection.

duration =
*Only the parameter name is used by Splunk. It must match what's returned by the executable through introspection.

